[package]
name = "task_ledger"
version = "0.1.0"
authors = ["Blockchain-Oracle"]
edition = "2021"
publish = false

[dependencies]
ink = { version = "5.1.1", default-features = false }

[dev-dependencies]
ink_e2e = "5.1.1"

[lib]
path = "lib.rs"

[features]
default = ["std"]
std = ["ink/std"]
ink-as-dependency = []
e2e-tests = []
